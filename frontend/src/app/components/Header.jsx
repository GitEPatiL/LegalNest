import siteConfig from '@/config/siteConfig';

export default function Header() {
    return (
        <header>
            {/* Header component placeholder */}
            {/* Will use siteConfig.companyName, siteConfig.logo, etc. */}
        </header>
    );
}
