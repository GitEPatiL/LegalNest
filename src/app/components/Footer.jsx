import siteConfig from '@/config/siteConfig';

export default function Footer() {
    return (
        <footer>
            {/* Footer component placeholder */}
            {/* Will use siteConfig.email, siteConfig.contactNumber, siteConfig.social, etc. */}
        </footer>
    );
}
